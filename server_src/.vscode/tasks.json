{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build project", // We will refer to this name later
            "type": "shell",
            "command": "make",
            "args": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Runs the project Makefile"
        }
    ]
}